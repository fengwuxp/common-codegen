spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:file:${java.io.tmpdir}/h2_data:loong_codegen;MODE=MySQL
    platform: h2
    driver-class-name: org.h2.Driver

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
